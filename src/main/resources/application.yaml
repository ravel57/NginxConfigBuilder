server:
  port: 8080

spring:
  application:
    name: NginxConfigBuilder

credentials:
  login: ${LOGIN:admin}
  password: ${PASSWORD:admin}

certificate:
  email: ${EMAIL}

nginx:
  config-path: ${CONFIG_PATH:/etc/nginx/nginx.conf}